import type { FlexItemSystemProps } from '@/design-system/types';

export const flexItemSystemProps: Record<keyof FlexItemSystemProps, readonly string[]> = {
  flexBasis: ['flex-basis'],
  flex: ['flex'],
};
