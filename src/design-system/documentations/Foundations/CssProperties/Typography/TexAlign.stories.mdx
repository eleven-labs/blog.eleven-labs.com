import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { cssProperties } from '@/design-system/constants';

<Meta
    title="Foundations/Css Properties/Typography/TextAlign"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="CSS Properties"
    description="Text align CSS property scales"
>
    <DesignTokenTable
        title="Text Align"
        tokenVariables={cssProperties['text-align']}
    />
</Layout>
