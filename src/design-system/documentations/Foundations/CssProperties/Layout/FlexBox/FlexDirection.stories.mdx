import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { cssProperties } from '@/design-system/constants';

<Meta
    title="Foundations/Css Properties/Layout/FlexBox/FlexDirection"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="CSS Properties"
    description="Flex Direction CSS property scales"
>
    <DesignTokenTable
        title="Flex Direction"
        tokenVariables={cssProperties['flex-direction']}
    />
</Layout>
