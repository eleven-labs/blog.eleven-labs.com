import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { cssProperties } from '@/design-system/constants';

<Meta
    title="Foundations/Css Properties/Layout/FlexBox/JustifyContent"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="CSS Properties"
    description="Justify Content CSS property scales"
>
    <DesignTokenTable
        title="Justify Content"
        tokenVariables={cssProperties['justify-content']}
    />
</Layout>
