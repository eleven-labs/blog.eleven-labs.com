import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { cssProperties } from '@/design-system/constants';

<Meta
    title="Foundations/Css Properties/Layout/FlexBox/AlignSelf"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="CSS Properties"
    description="Align Self CSS property scales"
>
    <DesignTokenTable
        title="Align Self"
        tokenVariables={cssProperties['align-self']}
    />
</Layout>
