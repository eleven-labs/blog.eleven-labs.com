import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { tokenVariables } from '@/design-system/constants';

<Meta
    title="Foundations/Design Tokens/Layout/Width"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Layout"
    description="Width token scales"
>
    <DesignTokenTable
        title="Width"
        tokenVariables={tokenVariables['width']}
    />
</Layout>
