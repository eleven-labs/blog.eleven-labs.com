import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { Heading, headingSizeList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Typography/Heading"
    parameters={{
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } },
        backgrounds: { disable: true, grid: { disable: true } },
    }}
/>

<Layout
    title="Typography"
    description="Heading token scales, available through <code>text-heading-*</code> — or, better, through the <code>Heading</code> component, which also carries the family and the weight."
>
    <DesignTokenTable
        title="Heading"
        withDesktop
        tokens={headingSizeList.map((tokenName) => ({
            name: `heading-${tokenName}`,
            cssVariable: `--font-size-heading-${tokenName}`,
            preview: <Heading size={tokenName}>Title</Heading>,
        }))}
    />
</Layout>
