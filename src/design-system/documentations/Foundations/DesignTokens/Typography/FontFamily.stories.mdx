import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { fontFamilyTokenNameList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Typography/Font Family"
    parameters={{
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } },
        backgrounds: { disable: true, grid: { disable: true } },
    }}
/>

<Layout
    title="Typography"
    description="Font families, available through <code>font-base</code>, <code>font-heading</code> and <code>font-blockquote</code>"
>
    <DesignTokenTable
        title="Font family"
        tokens={fontFamilyTokenNameList.map((tokenName) => ({
            name: tokenName,
            cssVariable: `--font-${tokenName}`,
            preview: (
                <p className="text-m" style={{ fontFamily: `var(--font-${tokenName})` }}>
                    The quick brown fox jumps over the lazy dog
                </p>
            ),
        }))}
    />
</Layout>
