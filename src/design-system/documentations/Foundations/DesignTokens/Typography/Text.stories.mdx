import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { Text, textSizeList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Typography/Text"
    parameters={{
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } },
        backgrounds: { disable: true, grid: { disable: true } },
    }}
/>

<Layout
    title="Typography"
    description="Body text token scales, available through <code>text-xs</code>, <code>text-s</code> and <code>text-m</code>"
>
    <DesignTokenTable
        title="Text"
        withDesktop
        tokens={textSizeList.map((tokenName) => ({
            name: tokenName,
            cssVariable: `--font-size-text-${tokenName}`,
            preview: <Text size={tokenName}>The quick brown fox jumps over the lazy dog</Text>,
        }))}
    />
</Layout>
