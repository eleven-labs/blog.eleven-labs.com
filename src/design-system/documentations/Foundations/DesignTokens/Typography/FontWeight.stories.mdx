import { Meta } from '@storybook/addon-docs';
import { Layout, Table } from '@/storybook/components';
import { Text } from '@/design-system';
import { tokenVariables } from '@/design-system/constants';

<Meta
    title="Foundations/Design Tokens/Typography/FontWeight"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Font weight token scales"
>
    <Table
        title="Font Weight"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'value',
                label: 'Value',
            },
        ]}
        rows={Object.entries(tokenVariables['font-weight']).map(([tokenName, token]) => ({
            name: (
                <Text fontWeight="medium">{tokenName}</Text>
            ),
            value: token.value,
        }))}
    />
</Layout>
