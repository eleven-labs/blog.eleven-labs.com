import { Meta } from '@storybook/addon-docs';
import { Layout, Table } from '@/storybook/components';
import { Text, fontWeightList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Typography/Font Weight"
    parameters={{
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } },
        backgrounds: { disable: true, grid: { disable: true } },
    }}
/>

<Layout
    title="Typography"
    description="Font weights. Tailwind's own scale is used as is: <code>font-normal</code>, <code>font-medium</code>, <code>font-semibold</code> and <code>font-bold</code>."
>
    <Table
        title="Font weight"
        columns={[
            { name: 'name', label: 'Class' },
            { name: 'preview', label: 'Preview' },
            { name: 'value', label: 'Value' },
        ]}
        rows={fontWeightList.map(({ name, value }) => ({
            name: <Text className="font-medium">font-{name}</Text>,
            preview: (
                <p className="text-m" style={{ fontWeight: value }}>
                    The quick brown fox jumps over the lazy dog
                </p>
            ),
            value,
        }))}
    />
</Layout>
