import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { tokenVariables } from '@/design-system/constants';

<Meta
    title="Foundations/Design Tokens/Typography/LineHeight"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Line weight token scales"
>
    <DesignTokenTable
        title="Line Height"
        tokenVariables={tokenVariables['line-height']}
    />
</Layout>
