import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { radiusTokenNameList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Radius"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Radius"
    description="Corner radii, available through <code>rounded-*</code>"
>
    <DesignTokenTable
        tokens={radiusTokenNameList.map((tokenName) => ({
            name: tokenName,
            cssVariable: `--radius-${tokenName}`,
            preview: (
                <div
                    className="size-[64px] bg-primary"
                    style={{ borderRadius: `var(--radius-${tokenName})` }}
                />
            ),
        }))}
    />
</Layout>
