import { Meta } from '@storybook/addon-docs';
import { Layout, Table } from '@/storybook/components';
import { Text, Icon, iconNameList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Icon"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Icon"
    description="All available icons"
>
    <Table
        title="Common"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            }
        ]}
        rows={iconNameList.map((iconName) => ({
          name: (
              <Text className="font-medium">{iconName}</Text>
          ),
          preview: <Icon name={iconName} size="5rem" className="text-primary" />,
        }))}
    />
</Layout>
