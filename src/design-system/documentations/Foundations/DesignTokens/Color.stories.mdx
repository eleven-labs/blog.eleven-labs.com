import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { colorTokenNameList } from '@/design-system';

<Meta
  title="Foundations/Design Tokens/Color"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      disable: true,
      grid: {
        disable: true,
      }
    },
  }}
/>

<Layout
  title="Color"
  description="Color palettes used by both design and engineering"
>
  <DesignTokenTable
    tokens={colorTokenNameList.map((tokenName) => ({
      name: tokenName,
      cssVariable: `--color-${tokenName}`,
      preview: (
        <div
          className="box-border h-[50px] w-[200px] border border-black"
          style={{ backgroundColor: `var(--color-${tokenName})` }}
        />
      ),
    }))}
  />
</Layout>
