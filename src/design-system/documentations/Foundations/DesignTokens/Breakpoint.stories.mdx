import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { breakpointTokenNameList } from '@/design-system';

<Meta
    title="Foundations/Design Tokens/Breakpoint"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Breakpoint"
    description="The blog is written mobile first: <code>xs</code> has no minimum width, and every other breakpoint is a <code>min-width</code> variant — <code>md:flex-row</code>. The <code>max-*</code> variants target everything below one of them."
>
    <DesignTokenTable
        tokens={breakpointTokenNameList.map((tokenName) => ({
            name: tokenName,
            cssVariable: `--breakpoint-${tokenName}`,
        }))}
    />
</Layout>
